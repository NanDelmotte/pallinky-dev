
{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/mobile"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^5.0.2"
  }
}